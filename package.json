{
  "name": "readme-generator",
  "version": "1.0.0",
  "description": "an application that generates professional readme based on users' inputs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hanh Le",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^6.3.1"
  }
}
